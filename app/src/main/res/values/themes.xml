<resources xmlns:tools="http://schemas.android.com/tools">
    <!--
        Base application theme.
        UI sepenuhnya memakai Jetpack Compose Material3, jadi theme XML cukup
        framework sederhana (tanpa ActionBar) sebagai fondasi Activity.
    -->
    <style name="Base.Theme.Budgetin" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:windowLightStatusBar">true</item>
    </style>

    <style name="Theme.Budgetin" parent="Base.Theme.Budgetin" />
</resources>
